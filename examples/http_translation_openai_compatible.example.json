{
  "id": "qwen-openai-compatible",
  "stage": "translation",
  "method": "POST",
  "url": "{{ base_url }}/v1/chat/completions",
  "headers": {
    "Authorization": "Bearer {{ api_key }}"
  },
  "request_template": {
    "model": "{{ model }}",
    "temperature": 0.4,
    "response_format": {
      "type": "json_object"
    },
    "messages": [
      {
        "role": "system",
        "content": "你是影视配音翻译助手。请把英/日/韩台词改写成自然中文配音台词，保留必要语气词、停顿感、人物情绪和说话风格。只输出 JSON 对象，字段为 target_text、emotion、style_prompt。"
      },
      {
        "role": "user",
        "content": "{{ prompt }}\n\nsource_language={{ source_language }}\ntarget_language={{ target_language }}\nspeaker_id={{ speaker_id }}\nduration_ms={{ duration_ms }}\nsource_text={{ segment_text }}"
      }
    ]
  },
  "response_mapping": {
    "content_json": "$.choices[0].message.content"
  },
  "timeout_seconds": 120
}
